import React, { useState, useEffect } from "react";
import { Circle, Square, RectangleHorizontal, RectangleVertical, Triangle, PlaySquare, Component, Pill, Hexagon, Calculator , Save } from "lucide-react";
import { motion } from "motion/react";
import ShareButtonWithPopup from "./ShareMenu";
import { saveEstimate } from "../../lib/estimates";
import { useAuth } from "../../contexts/AuthContext";
import { useSettings } from "../../context/SettingsContext"; type Shape = "Circle" | "Square" | "Rectangle" | "Triangle" | "Trapezoid" | "Ellipse" | "RightTriangle" | "HorizontalCapsule" | "VerticalCapsule" | "Parallelogram" | "IrregularQuad";
type InputUnit = "mm" | "cm" | "m" | "inches" | "feet";
type OutputUnit = "sqm" | "sqft" | "acres" | "hectares"; export default function AreaCalculator() { const { settings, updateSettings } = useSettings(); const isMetric = settings.measurement === 'SI'; const { user } = useAuth(); const [isSaving, setIsSaving] = useState(false); const [saveMessage, setSaveMessage] = useState(""); const [activeShape, setActiveShape] = useState<Shape>("Rectangle"); const [inputUnit, setInputUnit] = useState<InputUnit>(isMetric ? "m" : "feet"); const [outputUnit, setOutputUnit] = useState<OutputUnit>(isMetric ? "sqm" : "sqft"); useEffect(() => { if (isMetric && ["feet", "inches"].includes(inputUnit)) { setInputUnit("m"); } else if (!isMetric && ["m", "cm", "mm"].includes(inputUnit)) { setInputUnit("feet"); } if (isMetric && ["sqft", "acres"].includes(outputUnit)) { setOutputUnit("sqm"); } else if (!isMetric && ["sqm", "hectares"].includes(outputUnit)) { setOutputUnit("sqft"); } }, [isMetric]); /* Inputs */  const [radius, setRadius] = useState(""); const [side, setSide] = useState(""); const [length, setLength] = useState(""); const [width, setWidth] = useState(""); /* Triangle (base, height, side2, side3) */  const [triBase, setTriBase] = useState(""); const [triHeight, setTriHeight] = useState(""); const [triSide2, setTriSide2] = useState(""); const [triSide3, setTriSide3] = useState(""); /* Trapezoid (base1, base2, height, side1, side2) */  const [trapBase1, setTrapBase1] = useState(""); const [trapBase2, setTrapBase2] = useState(""); const [trapHeight, setTrapHeight] = useState(""); const [trapSide1, setTrapSide1] = useState(""); const [trapSide2, setTrapSide2] = useState(""); /* Ellipse (semi-major, semi-minor) */  const [ellMajor, setEllMajor] = useState(""); const [ellMinor, setEllMinor] = useState(""); /* Right Triangle */  const [rtBase, setRtBase] = useState(""); const [rtHeight, setRtHeight] = useState(""); /* Capsule */  const [capLength, setCapLength] = useState(""); /* straight part */  const [capRadius, setCapRadius] = useState(""); /* Vertical Capsule */  const [vCapLength, setVCapLength] = useState(""); const [vCapRadius, setVCapRadius] = useState(""); /* Parallelogram */  const [paraBase, setParaBase] = useState(""); const [paraSide, setParaSide] = useState(""); const [paraHeight, setParaHeight] = useState(""); /* Irregular Quadrilateral */  const [quadA, setQuadA] = useState(""); const [quadB, setQuadB] = useState(""); const [quadC, setQuadC] = useState(""); const [quadD, setQuadD] = useState(""); const [quadDiag, setQuadDiag] = useState(""); const shapes: { id: Shape; label: string; icon: any; color: string }[] = [ { id: "Circle", label: "Circle", icon: Circle, color: "text-rose-500 bg-rose-100 dark:bg-rose-500/20" }, { id: "Square", label: "Square", icon: Square, color: "text-blue-500 bg-blue-100 dark:bg-blue-500/20" }, { id: "Rectangle", label: "Rectangle", icon: RectangleHorizontal, color: "text-emerald-500 bg-emerald-100 dark:bg-emerald-500/20" }, { id: "Triangle", label: "Triangle", icon: Triangle, color: "text-amber-500 bg-amber-100 dark:bg-amber-500/20" }, { id: "Trapezoid", label: "Trapezoid", icon: PlaySquare, color: "text-violet-500 bg-violet-100 dark:bg-violet-500/20" }, /* Approximation */ { id: "Ellipse", label: "Ellipse", icon: Circle, color: "text-pink-500 bg-pink-100 dark:bg-pink-500/20" }, /* Approximation */ { id: "RightTriangle", label: "Shape 1", icon: Triangle, color: "text-orange-500 bg-orange-100 dark:bg-orange-500/20" }, { id: "HorizontalCapsule", label: "Shape 2", icon: Pill, color: "text-cyan-500 bg-cyan-100 dark:bg-cyan-500/20" }, { id: "VerticalCapsule", label: "Shape 3", icon: RectangleVertical, color: "text-fuchsia-500 bg-fuchsia-100 dark:bg-fuchsia-500/20" }, { id: "Parallelogram", label: "Shape 4", icon: Component, color: "text-lime-500 bg-lime-100 dark:bg-lime-500/20" }, { id: "IrregularQuad", label: "Shape 5", icon: Hexagon, color: "text-indigo-500 bg-indigo-100 dark:bg-indigo-500/20" }, ]; /* Convert input value built in input unit to meters */ const toMeters = (val: string | number) => { const num = typeof val === "string" ? parseFloat(val) : val; if (isNaN(num)) return 0; switch (inputUnit) { case "mm": return num / 1000; case "cm": return num / 100; case "inches": return num * 0.0254; case "feet": return num * 0.3048; case "m": default: return num; } }; /* Convert area in sq. meters to target output unit */ const formatArea = (areaSqM: number) => { switch (outputUnit) { case "sqft": return (areaSqM * 10.7639).toFixed(2) + " sq.ft"; case "acres": return (areaSqM * 0.000247105).toFixed(4) + " acres"; case "hectares": return (areaSqM * 0.0001).toFixed(4) + " hectares"; case "sqm": default: return areaSqM.toFixed(2) + " m²"; } }; /* Convert perimeter in meters to input unit (usually perimeter is shown in same unit as input, or user might want something else, let's keep it in input unit or meters) */ const formatPerimeter = (perimMeters: number) => { switch (inputUnit) { case "mm": return (perimMeters * 1000).toFixed(2) + " mm"; case "cm": return (perimMeters * 100).toFixed(2) + " cm"; case "inches": return (perimMeters / 0.0254).toFixed(2) + " inches"; case "feet": return (perimMeters / 0.3048).toFixed(2) + " feet"; case "m": default: return perimMeters.toFixed(2) + " m"; } }; let calcAreaSqM = 0; let calcPerimeterM = 0; let inputSummary: Record<string, string> = {}; if (activeShape === "Circle") { const r = toMeters(radius); calcAreaSqM = Math.PI * r * r; calcPerimeterM = 2 * Math.PI * r; inputSummary = { "Radius": `${radius} ${inputUnit}` }; } else if (activeShape === "Square") { const s = toMeters(side); calcAreaSqM = s * s; calcPerimeterM = 4 * s; inputSummary = { "Side": `${side} ${inputUnit}` }; } else if (activeShape === "Rectangle") { const l = toMeters(length); const w = toMeters(width); calcAreaSqM = l * w; calcPerimeterM = 2 * (l + w); inputSummary = { "Length": `${length} ${inputUnit}`, "Width": `${width} ${inputUnit}` }; } else if (activeShape === "Triangle") { const b = toMeters(triBase); const h = toMeters(triHeight); const s2 = toMeters(triSide2); const s3 = toMeters(triSide3); calcAreaSqM = 0.5 * b * h; calcPerimeterM = b + s2 + s3; inputSummary = { "Base": `${triBase} ${inputUnit}`, "Height": `${triHeight} ${inputUnit}`, "Side 2": `${triSide2} ${inputUnit}`, "Side 3": `${triSide3} ${inputUnit}` }; } else if (activeShape === "Trapezoid") { const b1 = toMeters(trapBase1); const b2 = toMeters(trapBase2); const h = toMeters(trapHeight); const s1 = toMeters(trapSide1); const s2 = toMeters(trapSide2); calcAreaSqM = 0.5 * (b1 + b2) * h; calcPerimeterM = b1 + b2 + s1 + s2; inputSummary = { "Base 1": `${trapBase1} ${inputUnit}`, "Base 2": `${trapBase2} ${inputUnit}`, "Height": `${trapHeight} ${inputUnit}`, "Leg 1": `${trapSide1} ${inputUnit}`, "Leg 2": `${trapSide2} ${inputUnit}` }; } else if (activeShape === "Ellipse") { const a = toMeters(ellMajor); const b = toMeters(ellMinor); calcAreaSqM = Math.PI * a * b; /* Ramanujan's approximation for ellipse perimeter */ calcPerimeterM = Math.PI * (3 * (a + b) - Math.sqrt((3 * a + b) * (a + 3 * b))); inputSummary = { "Semi-major Axis (a)": `${ellMajor} ${inputUnit}`, "Semi-minor Axis (b)": `${ellMinor} ${inputUnit}` }; } else if (activeShape === "RightTriangle") { const b = toMeters(rtBase); const h = toMeters(rtHeight); calcAreaSqM = 0.5 * b * h; calcPerimeterM = b + h + Math.sqrt(b * b + h * h); inputSummary = { "Base": `${rtBase} ${inputUnit}`, "Height": `${rtHeight} ${inputUnit}` }; } else if (activeShape === "HorizontalCapsule") { const l = toMeters(capLength); const r = toMeters(capRadius); calcAreaSqM = (l * 2 * r) + (Math.PI * r * r); calcPerimeterM = (2 * l) + (2 * Math.PI * r); inputSummary = { "Straight Length": `${capLength} ${inputUnit}`, "Radius": `${capRadius} ${inputUnit}` }; } else if (activeShape === "VerticalCapsule") { const l = toMeters(vCapLength); const r = toMeters(vCapRadius); calcAreaSqM = (l * 2 * r) + (Math.PI * r * r); calcPerimeterM = (2 * l) + (2 * Math.PI * r); inputSummary = { "Straight Length": `${vCapLength} ${inputUnit}`, "Radius": `${vCapRadius} ${inputUnit}` }; } else if (activeShape === "Parallelogram") { const b = toMeters(paraBase); const h = toMeters(paraHeight); const s = toMeters(paraSide); calcAreaSqM = b * h; calcPerimeterM = 2 * (b + s); inputSummary = { "Base": `${paraBase} ${inputUnit}`, "Height": `${paraHeight} ${inputUnit}`, "Side": `${paraSide} ${inputUnit}` }; } else if (activeShape === "IrregularQuad") { const a = toMeters(quadA); const b = toMeters(quadB); const c = toMeters(quadC); const d = toMeters(quadD); const diag = toMeters(quadDiag); /* Area via two triangles using Heron's formula */  const s1 = (a + b + diag) / 2; const area1 = Math.sqrt(Math.max(0, s1 * (s1 - a) * (s1 - b) * (s1 - diag))); const s2 = (c + d + diag) / 2; const area2 = Math.sqrt(Math.max(0, s2 * (s2 - c) * (s2 - d) * (s2 - diag))); calcAreaSqM = area1 + area2; calcPerimeterM = a + b + c + d; inputSummary = { "Side A": `${quadA} ${inputUnit}`, "Side B": `${quadB} ${inputUnit}`, "Side C": `${quadC} ${inputUnit}`, "Side D": `${quadD} ${inputUnit}`, "Diagonal": `${quadDiag} ${inputUnit}` }; } const exportData = { "Shape": activeShape, "Calculated Area": formatArea(calcAreaSqM), "Calculated Perimeter": formatPerimeter(calcPerimeterM) }; return ( <div className="w-full h-full overflow-y-auto bg-slate-50 dark:bg-slate-950 text-slate-900 dark:text-white p-6 md:p-8"> <div className="max-w-4xl mx-auto"> <h1 className="text-3xl font-black mb-2 flex items-center gap-3 whitespace-nowrap"> <Calculator className="w-8 h-8 text-indigo-500" /> Area Calculator </h1> <p className="text-slate-500 dark:text-slate-400 mb-8 font-medium">Calculate area and perimeter for any 2D shape with unit conversions.</p> {/* Global Settings */} <div className="flex flex-wrap gap-4 mb-8"> <div> <label className="text-xs font-bold text-gray-500 uppercase block mb-1 whitespace-nowrap">Input Unit</label> <select value={inputUnit} onChange={e => { const newU = e.target.value as InputUnit; setInputUnit(newU); if (newU === "feet" || newU === "inches") { updateSettings({ measurement: "FPS" }); } else if (newU === "m" || newU === "cm" || newU === "mm") { updateSettings({ measurement: "SI" }); } }} className="bg-white dark:bg-slate-800 border dark:border-slate-700 p-2 rounded-xl text-sm font-medium" > <option value="mm">Millimeters (mm)</option> <option value="cm">Centimeters (cm)</option> <option value="m">Meters (m)</option> <option value="inches">Inches (in)</option> <option value="feet">Feet (ft)</option> </select> </div> <div> <label className="text-xs font-bold text-gray-500 uppercase block mb-1 whitespace-nowrap">Area Output Unit</label> <select value={outputUnit} onChange={e => { const newU = e.target.value as OutputUnit; setOutputUnit(newU); if (newU === "sqft" || newU === "acres") { updateSettings({ measurement: "FPS" }); } else if (newU === "sqm" || newU === "hectares") { updateSettings({ measurement: "SI" }); } }} className="bg-white dark:bg-slate-800 border dark:border-slate-700 p-2 rounded-xl text-sm font-medium" > <option value="sqm">Square Meters (m²)</option> <option value="sqft">Square Feet (sq.ft)</option> <option value="acres">Acres</option> <option value="hectares">Hectares</option> </select> </div> </div> {/* Shapes Grid */} <div className="grid grid-cols-2 sm:grid-cols-3 md:grid-cols-6 gap-3 mb-8"> {shapes.map((s) => { const Icon = s.icon; const isActive = activeShape === s.id; const baseColor = s.color.split('-')[1]; return ( <button key={s.id} onClick={() => setActiveShape(s.id)} className={`relative flex flex-col items-center justify-center gap-2 p-4 rounded-[20px] transition-all duration-200 overflow-hidden group hover:border-[color:var(--theme-color)] hover:bg-[color:var(--theme-bg-light)] ${isActive ? 'shadow-sm' : 'border border-slate-200 dark:border-slate-800 bg-white dark:bg-slate-900'}`} style={{ '--theme-color': `var(--color-${baseColor}-500)`, '--theme-color-hover': `var(--color-${baseColor}-600)`, '--theme-bg': `color-mix(in srgb, var(--color-${baseColor}-500) 10%, transparent)`, '--theme-bg-light': `color-mix(in srgb, var(--color-${baseColor}-500) 5%, transparent)`, borderColor: isActive ? 'var(--theme-color)' : undefined, borderWidth: isActive ? '2px' : undefined, borderStyle: isActive ? 'solid' : undefined, backgroundColor: isActive ? 'var(--theme-bg-light)' : undefined } as React.CSSProperties} > <motion.div className="flex items-center justify-center w-12 h-12 rounded-xl" animate={isActive ? { scale: [1, 1.15, 1], y: [0, -3, 0] } : { scale: 1, y: 0 }} transition={{ duration: 0.4, ease: "easeOut" }} whileHover={!isActive ? { scale: 1.1 } : {}} style={{ backgroundColor: 'var(--theme-bg)', color: 'var(--theme-color)', filter: isActive ? 'drop-shadow(0 0 20px color-mix(in srgb, var(--theme-color) 25%, transparent))' : undefined }} > <Icon className="w-7 h-7" strokeWidth={2} /> </motion.div> <motion.span animate={isActive ? { scale: [1, 1.05, 1] } : { scale: 1 }} transition={{ duration: 0.4, ease: "easeOut" }} className={`text-[11px] font-extrabold text-center leading-tight tracking-wide ${isActive ? '' : 'text-slate-600 dark:text-slate-400 group-hover:[color:var(--theme-color-hover)]'}`} style={{ color: isActive ? 'var(--theme-color-hover)' : undefined }} > {s.label} </motion.span> </button> ) })} </div> <div className="grid grid-cols-1 md:grid-cols-2 gap-8"> {/* Inputs Section */} <div className="bg-white dark:bg-slate-900 p-6 rounded-[2rem] border border-slate-200 dark:border-slate-800"> <h3 className="font-bold text-lg mb-6 whitespace-nowrap">{activeShape} Measurements</h3> <div className="space-y-4"> {activeShape === "Circle" && ( <div> <label className="text-xs font-bold text-gray-500 uppercase whitespace-nowrap">Radius ({inputUnit})</label> <input type="number" value={radius} onChange={e => setRadius(e.target.value)} className="w-full bg-slate-50 dark:bg-slate-800 border-none p-3 rounded-xl mt-1 font-medium focus:ring-2 focus:ring-indigo-500" /> </div> )} {activeShape === "Square" && ( <div> <label className="text-xs font-bold text-gray-500 uppercase whitespace-nowrap">Side length ({inputUnit})</label> <input type="number" value={side} onChange={e => setSide(e.target.value)} className="w-full bg-slate-50 dark:bg-slate-800 border-none p-3 rounded-xl mt-1 font-medium focus:ring-2 focus:ring-indigo-500" /> </div> )} {activeShape === "Rectangle" && ( <> <div> <label className="text-xs font-bold text-gray-500 uppercase whitespace-nowrap">Length ({inputUnit})</label> <input type="number" value={length} onChange={e => setLength(e.target.value)} className="w-full bg-slate-50 dark:bg-slate-800 border-none p-3 rounded-xl mt-1 font-medium focus:ring-2 focus:ring-indigo-500" /> </div> <div> <label className="text-xs font-bold text-gray-500 uppercase whitespace-nowrap">Width ({inputUnit})</label> <input type="number" value={width} onChange={e => setWidth(e.target.value)} className="w-full bg-slate-50 dark:bg-slate-800 border-none p-3 rounded-xl mt-1 font-medium focus:ring-2 focus:ring-indigo-500" /> </div> </> )} {activeShape === "Triangle" && ( <> <div className="grid grid-cols-2 gap-4"> <div> <label className="text-xs font-bold text-gray-500 uppercase whitespace-nowrap">Base ({inputUnit})</label> <input type="number" value={triBase} onChange={e => setTriBase(e.target.value)} className="w-full bg-slate-50 dark:bg-slate-800 border-none p-3 rounded-xl mt-1 font-medium focus:ring-2 focus:ring-indigo-500" /> </div> <div> <label className="text-xs font-bold text-gray-500 uppercase whitespace-nowrap">Height ({inputUnit})</label> <input type="number" value={triHeight} onChange={e => setTriHeight(e.target.value)} className="w-full bg-slate-50 dark:bg-slate-800 border-none p-3 rounded-xl mt-1 font-medium focus:ring-2 focus:ring-indigo-500" /> </div> </div> <div className="grid grid-cols-2 gap-4"> <div> <label className="text-xs font-bold text-gray-500 uppercase whitespace-nowrap">Side 2 ({inputUnit})</label> <input type="number" value={triSide2} onChange={e => setTriSide2(e.target.value)} className="w-full bg-slate-50 dark:bg-slate-800 border-none p-3 rounded-xl mt-1 font-medium focus:ring-2 focus:ring-indigo-500" /> </div> <div> <label className="text-xs font-bold text-gray-500 uppercase whitespace-nowrap">Side 3 ({inputUnit})</label> <input type="number" value={triSide3} onChange={e => setTriSide3(e.target.value)} className="w-full bg-slate-50 dark:bg-slate-800 border-none p-3 rounded-xl mt-1 font-medium focus:ring-2 focus:ring-indigo-500" /> </div> </div> </> )} {activeShape === "Trapezoid" && ( <> <div className="grid grid-cols-2 gap-4"> <div> <label className="text-xs font-bold text-gray-500 uppercase whitespace-nowrap">Parallel Base 1 ({inputUnit})</label> <input type="number" value={trapBase1} onChange={e => setTrapBase1(e.target.value)} className="w-full bg-slate-50 dark:bg-slate-800 border-none p-3 rounded-xl mt-1 font-medium focus:ring-2 focus:ring-indigo-500" /> </div> <div> <label className="text-xs font-bold text-gray-500 uppercase whitespace-nowrap">Parallel Base 2 ({inputUnit})</label> <input type="number" value={trapBase2} onChange={e => setTrapBase2(e.target.value)} className="w-full bg-slate-50 dark:bg-slate-800 border-none p-3 rounded-xl mt-1 font-medium focus:ring-2 focus:ring-indigo-500" /> </div> </div> <div> <label className="text-xs font-bold text-gray-500 uppercase whitespace-nowrap">Height ({inputUnit})</label> <input type="number" value={trapHeight} onChange={e => setTrapHeight(e.target.value)} className="w-full bg-slate-50 dark:bg-slate-800 border-none p-3 rounded-xl mt-1 font-medium focus:ring-2 focus:ring-indigo-500" /> </div> <div className="grid grid-cols-2 gap-4"> <div> <label className="text-xs font-bold text-gray-500 uppercase whitespace-nowrap">Leg 1 ({inputUnit})</label> <input type="number" value={trapSide1} onChange={e => setTrapSide1(e.target.value)} className="w-full bg-slate-50 dark:bg-slate-800 border-none p-3 rounded-xl mt-1 font-medium focus:ring-2 focus:ring-indigo-500" /> </div> <div> <label className="text-xs font-bold text-gray-500 uppercase whitespace-nowrap">Leg 2 ({inputUnit})</label> <input type="number" value={trapSide2} onChange={e => setTrapSide2(e.target.value)} className="w-full bg-slate-50 dark:bg-slate-800 border-none p-3 rounded-xl mt-1 font-medium focus:ring-2 focus:ring-indigo-500" /> </div> </div> </> )} {activeShape === "Ellipse" && ( <> <div> <label className="text-xs font-bold text-gray-500 uppercase whitespace-nowrap">Semi-major Axis (a) ({inputUnit})</label> <input type="number" value={ellMajor} onChange={e => setEllMajor(e.target.value)} className="w-full bg-slate-50 dark:bg-slate-800 border-none p-3 rounded-xl mt-1 font-medium focus:ring-2 focus:ring-indigo-500" /> </div> <div> <label className="text-xs font-bold text-gray-500 uppercase whitespace-nowrap">Semi-minor Axis (b) ({inputUnit})</label> <input type="number" value={ellMinor} onChange={e => setEllMinor(e.target.value)} className="w-full bg-slate-50 dark:bg-slate-800 border-none p-3 rounded-xl mt-1 font-medium focus:ring-2 focus:ring-indigo-500" /> </div> </> )} {activeShape === "RightTriangle" && ( <div className="grid grid-cols-2 gap-4"> <div> <label className="text-xs font-bold text-gray-500 uppercase whitespace-nowrap">Base ({inputUnit})</label> <input type="number" value={rtBase} onChange={e => setRtBase(e.target.value)} className="w-full bg-slate-50 dark:bg-slate-800 border-none p-3 rounded-xl mt-1 font-medium focus:ring-2 focus:ring-indigo-500" /> </div> <div> <label className="text-xs font-bold text-gray-500 uppercase whitespace-nowrap">Height ({inputUnit})</label> <input type="number" value={rtHeight} onChange={e => setRtHeight(e.target.value)} className="w-full bg-slate-50 dark:bg-slate-800 border-none p-3 rounded-xl mt-1 font-medium focus:ring-2 focus:ring-indigo-500" /> </div> </div> )} {activeShape === "HorizontalCapsule" && ( <div className="grid grid-cols-2 gap-4"> <div> <label className="text-xs font-bold text-gray-500 uppercase whitespace-nowrap">Straight Length ({inputUnit})</label> <input type="number" value={capLength} onChange={e => setCapLength(e.target.value)} className="w-full bg-slate-50 dark:bg-slate-800 border-none p-3 rounded-xl mt-1 font-medium focus:ring-2 focus:ring-indigo-500" /> </div> <div> <label className="text-xs font-bold text-gray-500 uppercase whitespace-nowrap">Radius ({inputUnit})</label> <input type="number" value={capRadius} onChange={e => setCapRadius(e.target.value)} className="w-full bg-slate-50 dark:bg-slate-800 border-none p-3 rounded-xl mt-1 font-medium focus:ring-2 focus:ring-indigo-500" /> </div> </div> )} {activeShape === "VerticalCapsule" && ( <div className="grid grid-cols-2 gap-4"> <div> <label className="text-xs font-bold text-gray-500 uppercase whitespace-nowrap">Straight Length ({inputUnit})</label> <input type="number" value={vCapLength} onChange={e => setVCapLength(e.target.value)} className="w-full bg-slate-50 dark:bg-slate-800 border-none p-3 rounded-xl mt-1 font-medium focus:ring-2 focus:ring-indigo-500" /> </div> <div> <label className="text-xs font-bold text-gray-500 uppercase whitespace-nowrap">Radius ({inputUnit})</label> <input type="number" value={vCapRadius} onChange={e => setVCapRadius(e.target.value)} className="w-full bg-slate-50 dark:bg-slate-800 border-none p-3 rounded-xl mt-1 font-medium focus:ring-2 focus:ring-indigo-500" /> </div> </div> )} {activeShape === "Parallelogram" && ( <> <div className="grid grid-cols-2 gap-4"> <div> <label className="text-xs font-bold text-gray-500 uppercase whitespace-nowrap">Base ({inputUnit})</label> <input type="number" value={paraBase} onChange={e => setParaBase(e.target.value)} className="w-full bg-slate-50 dark:bg-slate-800 border-none p-3 rounded-xl mt-1 font-medium focus:ring-2 focus:ring-indigo-500" /> </div> <div> <label className="text-xs font-bold text-gray-500 uppercase whitespace-nowrap">Side ({inputUnit})</label> <input type="number" value={paraSide} onChange={e => setParaSide(e.target.value)} className="w-full bg-slate-50 dark:bg-slate-800 border-none p-3 rounded-xl mt-1 font-medium focus:ring-2 focus:ring-indigo-500" /> </div> </div> <div> <label className="text-xs font-bold text-gray-500 uppercase whitespace-nowrap">Vertical Height ({inputUnit})</label> <input type="number" value={paraHeight} onChange={e => setParaHeight(e.target.value)} className="w-full bg-slate-50 dark:bg-slate-800 border-none p-3 rounded-xl mt-1 font-medium focus:ring-2 focus:ring-indigo-500" /> </div> </> )} {activeShape === "IrregularQuad" && ( <> <div className="grid grid-cols-2 gap-4"> <div> <label className="text-xs font-bold text-gray-500 uppercase whitespace-nowrap">Side A ({inputUnit})</label> <input type="number" value={quadA} onChange={e => setQuadA(e.target.value)} className="w-full bg-slate-50 dark:bg-slate-800 border-none p-3 rounded-xl mt-1 font-medium focus:ring-2 focus:ring-indigo-500" /> </div> <div> <label className="text-xs font-bold text-gray-500 uppercase whitespace-nowrap">Side B ({inputUnit})</label> <input type="number" value={quadB} onChange={e => setQuadB(e.target.value)} className="w-full bg-slate-50 dark:bg-slate-800 border-none p-3 rounded-xl mt-1 font-medium focus:ring-2 focus:ring-indigo-500" /> </div> </div> <div className="grid grid-cols-2 gap-4"> <div> <label className="text-xs font-bold text-gray-500 uppercase whitespace-nowrap">Side C ({inputUnit})</label> <input type="number" value={quadC} onChange={e => setQuadC(e.target.value)} className="w-full bg-slate-50 dark:bg-slate-800 border-none p-3 rounded-xl mt-1 font-medium focus:ring-2 focus:ring-indigo-500" /> </div> <div> <label className="text-xs font-bold text-gray-500 uppercase whitespace-nowrap">Side D ({inputUnit})</label> <input type="number" value={quadD} onChange={e => setQuadD(e.target.value)} className="w-full bg-slate-50 dark:bg-slate-800 border-none p-3 rounded-xl mt-1 font-medium focus:ring-2 focus:ring-indigo-500" /> </div> </div> <div> <label className="text-xs font-bold text-gray-500 uppercase whitespace-nowrap">Diagonal (A to C) ({inputUnit})</label> <input type="number" value={quadDiag} onChange={e => setQuadDiag(e.target.value)} className="w-full bg-slate-50 dark:bg-slate-800 border-none p-3 rounded-xl mt-1 font-medium focus:ring-2 focus:ring-indigo-500" /> </div> </> )} </div> </div> {/* Results Section */} <div className="bg-slate-900 border border-slate-800 rounded-[2rem] p-6 lg:p-8 text-white shadow-xl flex flex-col justify-between"> <div> <h3 className="font-bold text-slate-300 text-sm uppercase tracking-widest mb-6 whitespace-nowrap">Real-Time Calculation</h3> <div className="space-y-6"> <div className="bg-slate-800/50 px-4 py-3 rounded-xl border border-slate-700 flex-1 min-w-fit whitespace-nowrap"> <span className="block text-slate-400 text-xs font-bold uppercase mb-1 whitespace-nowrap">Total Area</span> <span className="text-3xl font-black text-indigo-400 whitespace-nowrap">{formatArea(calcAreaSqM)}</span> </div> <div className="bg-slate-800/50 px-4 py-3 rounded-xl border border-slate-700 flex-1 min-w-fit whitespace-nowrap"> <span className="block text-slate-400 text-xs font-bold uppercase mb-1 whitespace-nowrap">Perimeter / Circumference</span> <span className="text-3xl font-black text-teal-400 whitespace-nowrap">{formatPerimeter(calcPerimeterM)}</span> </div> </div> </div> <div className="mt-6 flex flex-wrap gap-4 items-center"> <ShareButtonWithPopup activeTab="Area Calculator" title={`${activeShape} Area Calculation`} data={exportData} exportFormat={{ inputs: inputSummary, breakdown: exportData }} /> {user && ( <button onClick={async () => { setIsSaving(true); setSaveMessage(""); try { const payload = { inputs: inputSummary, breakdown: exportData }; const projName = prompt("Enter project element/estimate name:", "My AreaCalculator Estimate"); if (projName) { await saveEstimate(projName, payload); setSaveMessage("Saved successfully!"); setTimeout(() => setSaveMessage(""), 3000); } } catch (e) { setSaveMessage("Failed to save."); } finally { setIsSaving(false); } }} disabled={isSaving} className="bg-green-600/20 text-green-400 hover:bg-green-600/30 px-6 py-4 rounded-xl font-bold transition-colors shadow-sm flex items-center justify-center gap-2" > {isSaving ? ( <span className="animate-pulse">Saving...</span> ) : ( <> <Save className="w-5 h-5" /> Save to Profile </> )} </button> )} {saveMessage && <span className="text-sm font-bold text-green-400 ml-4">{saveMessage}</span>} </div> </div> </div> </div> </div> );
}
